# Changesets

Run `pnpm changeset` when a pull request changes a published package. Select the affected packages, choose the semantic version bump, and describe the user-facing change.

All published `@clankhouse` packages use a fixed version, so releasing any one of them releases the complete public package group at the same version.
