# `@clankhouse/server`

The Connect RPC server for exposing ClankHouse workflows to local and remote clients.

## Installation

```sh
pnpm add @clankhouse/server
```

ClankHouse is pre-0.1 and its APIs may change without notice.
